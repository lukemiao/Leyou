server:
  port: 10010

spring:
  application:
    name: lkm-zuul
zuul:
  routes:
    service-provider: /pro/**  # XX 路径名可以是任意的  不写就是 本身
    service-consumer: /con/**
  prefix: /api  #随便写 推荐api
#    service-consumer: /service-consumer/**
#    service-consumer:  # 路由名称，consumer的
#    service-provider:  # 路由名称，可以随便写，习惯上是服务名

#      path: /service-consumer/** # consumer的path
#      path: /service-provider/**

#      url: http://localhost # consumer的路径
#      url: http://localhost:8081
#      serviceId: service-provider #注册到eureka后 可用此方法
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
