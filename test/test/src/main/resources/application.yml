
spring:
  datasource:
    url: jdbc:mysql://192.168.1.28:3306/mybatis
    username: root
    password: 123456

  application:
    name: service-provider # 服务名称

mybatis:
  type-aliases-package: com.example.test.pojo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka

  instance:
    lease-expiration-duration-in-seconds: 10 # 过期时间
    lease-renewal-interval-in-seconds: 5 # 心跳时间
#    register-with-eureka: true  #是否给eureka注册容器 默认开启
server:
  port: 8081
